<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Vision Statement - Sprint 2</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      padding: 40px;
      color: #2c3e50;
    }
    .container {
      max-width: 900px;
      margin: 0 auto;
    }
    h1 {
      color: #16a085;
      font-size: 36px;
      text-align: center;
      margin-bottom: 40px;
      border-bottom: 3px solid #16a085;
      padding-bottom: 20px;
    }
    .vision-box {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 40px;
      border-radius: 12px;
      margin-bottom: 30px;
      font-size: 20px;
      line-height: 1.8;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }
    .dependencies {
      background: #ecf0f1;
      padding: 25px;
      border-radius: 8px;
      margin-top: 30px;
    }
    .dependencies h2 {
      color: #16a085;
      margin-bottom: 15px;
    }
    .dep-item {
      background: white;
      padding: 15px;
      margin-bottom: 10px;
      border-left: 4px solid #16a085;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Vision Statement</h1>

    <div class="vision-box">
      {{submission.data.vision_statement}}
    </div>

    {{#if dependencies}}
    <div class="dependencies">
      <h2>Built Upon</h2>
      {{#each dependencies}}
      <div class="dep-item">
        <strong>{{@key}}:</strong> {{this.value}}
      </div>
      {{/each}}
    </div>
    {{/if}}

    <div style="text-align: center; margin-top: 50px; color: #95a5a6; font-size: 12px;">
      Fast Track Tools | Sprint 2: Vision<br>
      {{generated_at}}
    </div>
  </div>
</body>
</html>
